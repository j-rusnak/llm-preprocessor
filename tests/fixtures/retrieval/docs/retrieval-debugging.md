# Retrieval Debugging

Use retrieval diagnostics when a query has a near miss, an unexpected rank, or
an empty top-k list. Each diagnostic row should include the original query, the
expected file, the expected rank when present, and whether graph expansion
changed the top-k result set.
